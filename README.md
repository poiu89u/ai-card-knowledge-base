# AI 通识卡片库

一个面向小白的 AI 通识知识库网站，使用 VitePress 构建。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## GitHub Pages 部署

推送到 GitHub 的 `main` 分支后，`.github/workflows/deploy.yml` 会自动构建并发布到 GitHub Pages。
